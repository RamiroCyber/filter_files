package constants

const TargetDir = "approved"

// LinuxCommands
const (
	Ls   = "ls"
	Echo = "echo"
	Grep = "grep"
	Cat  = "cat"
	Mv   = "mv"
)

const (
	V1      = "v1"
	API     = "/api"
	VERSION = "Version"
)

// LOGGER PREFIX
const (
	Error   = "ERROR"
	Info    = "INFO"
	Warning = "WARNING"
)
